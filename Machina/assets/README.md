# MACHINA assets

All graphics in MACHINA are generated at runtime via Phaser's Graphics API and
RenderTexture system (see `src/utils/ProceduralTextures.js`).
All sounds are synthesized at runtime via the Web Audio API
(see `src/utils/AudioManager.js`).

No external image, audio, or font files are required.
